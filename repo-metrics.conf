# Common configuration for the repo-metrics action.
# Use this file to pin tool versions and define default workflow values.

CLOC_STABLE_VERSION="1.94-1"
DEFAULT_BRANCH="main"
DEFAULT_OUTPUT_DIR="loc-reports"
